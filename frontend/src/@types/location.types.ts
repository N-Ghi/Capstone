export interface LocationData {
    place_name: string;
    latitude: string;
    longitude: string;
    place_id: string;
}

export interface Location {
    id: string;
    place_name: string;
    latitude: string;
    longitude: string;
    place_id: string;
}